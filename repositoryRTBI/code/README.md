# repositoryRTBI/code

This directory is intentionally empty.

The active RTBI API scripts are tracked in `repositoryRTBI/api/`:

- `main.py`
- `setup.sh`
- `test_api.sh`

This placeholder keeps the directory visible to Git and documents that no runtime scripts are expected here.
